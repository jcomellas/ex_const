locals_without_parens = [
  # Single constants
  const: 2,
  # Enumerated values
  enum: 2
]

[
  locals_without_parens: locals_without_parens,
  export: [
    locals_without_parens: locals_without_parens
  ]
]
